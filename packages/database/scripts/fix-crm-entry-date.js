/**
 * OBSOLETO: CrmCard.entryDate foi removido.
 * A data única de criação/entrada é CrmCard.createdAt (editável no formulário).
 */
console.log(
  'Script obsoleto: entryDate foi removido do schema. Use createdAt no CRM.',
);
process.exit(0);
